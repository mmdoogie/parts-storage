services:
  parts-storage:
    build: .
    ports:
      - "3002:3002"
      - "3003:3003"
    volumes:
      - parts-storage-data:/app/data
    environment:
      - MCP_DEBUG=false
    restart: unless-stopped

volumes:
  parts-storage-data:
